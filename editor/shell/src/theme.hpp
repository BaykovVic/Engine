#pragma once

class QApplication;

namespace sky::editor {

/// Applies the Unity-like dark theme (palette + stylesheet) to the app.
void applyDarkTheme(QApplication& app);

} // namespace sky::editor
