# Спринт 3. День 4

## feature/undo-scene-tests

- **Исполнитель:** E1 (Ядро и данные)
- **Порядок реализации:** 1
- **Зависимости:** `feature/undo-stack`, `feature/object-snapshot`, `feature/skyb-serialization`

**Цель фичи:** проверить отмену/повтор операций и сохранение/открытие сцены в SKYB.

**Описание фичи:** часть Этапа 3 (отмена операций и формат сцены SKYB) — тесты стека отмены и формата сцены; закрывает Этап 3 контура E1.

**Общий порядок реализации фичи:**
1. Написать проверки undo/redo в `undo_tests.cpp`.
2. Написать проверки сохранения/открытия сцены в `scene_tests.cpp`.

**Файлы фичи:**
1. `tests/undo_tests.cpp`
2. `tests/scene_tests.cpp`

### Файл: `tests/undo_tests.cpp`

**Назначение файла:** тесты отмены/повтора операций.

**Пошаговое описание действий:**
1. Проверить undo/redo для всех операций.

**Что должно быть в файле:**

*Структуры / классы / enum:* нет.

*Функции / методы:* тестовые функции.

*Логика функций / методов:* отмена/повтор всех операций (transform/field/create/delete/duplicate/reparent/rename).

**Результат по файлу:** зелёный тест `undo_tests`.

**Критерий правильности по файлу:**
1. undo/redo работают для transform/field/create/delete/duplicate/reparent/rename.

### Файл: `tests/scene_tests.cpp`

**Назначение файла:** тесты формата сцены SKYB.

**Пошаговое описание действий:**
1. Проверить `save → open` (граф, трансформы, поля компонентов).

**Что должно быть в файле:**

*Структуры / классы / enum:* нет.

*Функции / методы:* тестовые функции.

*Логика функций / методов:* сцена сохраняется в SKYB и открывается идентично; `save → open` восстанавливает граф объектов, трансформы и поля компонентов.

**Результат по файлу:** зелёный тест `scene_tests`.

**Критерий правильности по файлу:**
1. `save → open` восстанавливает граф объектов, трансформы и поля компонентов.

### На выходе должно получиться

**Список артефактов фичи:**
1. `tests/undo_tests.cpp`
2. `tests/scene_tests.cpp`
3. отмена/повтор всех операций; сцена сохраняется в SKYB и открывается идентично

**Общий критерий правильности:**
1. `save → open` восстанавливает граф объектов, трансформы и поля компонентов.
2. undo/redo работают для transform/field/create/delete/duplicate/reparent/rename.
