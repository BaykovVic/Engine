# Техническое задание · Контур E6 «Data-oriented системы (ECS)»

**Область ответственности.** ECS-мир, синхронизация с объектным миром, прикладные системы, многопоточность и профилирование. Обязательный минимум (этапы 1–2) — к 17 июля.

Все пути и имена методов взяты из фактического репозитория и совпадают с проектом 1:1. «Сделай файл» — файл создаётся на этом этапе; «Дополни файл» — в существующий файл добавляются перечисленные методы.


---

## Этап 1 (Неделя 1 · к 17 июля). ECS-мир и планировщик систем

**Общее описание задач контура.**

Реализовать ECS-ядро: сущности, хранилища компонентов, планировщик систем, запросы.

- **Сделай файл** `engine/ecs/include/sky/ecs/ecs.hpp`
  В файле должны быть: `IEcsComponentStore`, `componentType`, `has`, `remove`, `IEcsSystem`, `name`, `update`, `IEcsWorld`, `createEntity`, `destroyEntity`, `isAlive`, `store`, `IEcsSystemScheduler`, `registerSystem`, `unregisterSystem`, `tick`, `IEcsQueryService`
- **Сделай файл** `engine/ecs/include/sky/ecs/ecs_world.hpp`
  В файле должны быть: `set`, `get`, `EcsWorld`, `storeFor`, `stores`, `type_index`, `createEcsWorld`
- **Сделай файл** `engine/ecs/src/ecs_world.cpp`
  В файле должны быть: `createEntity`, `destroyEntity`, `remove`, `store`, `tick`, `update`, `has`, `createEcsWorld`

**На выходе должно получиться (список артефактов):**
- Библиотека `sky_ecs` собрана; ECS-часть тестов `runtime_tests`/`world_tests` зелёная.

**Критерий правильности:** Создание/уничтожение сущности; система обновляет только сущности с нужным компонентом; запрос по типам корректен.


---

## Этап 2 (Неделя 2 · к 17 июля). Синхронизация с объектным миром

**Общее описание задач контура.**

Реализовать явный контракт синхронизации ECS с объектным миром (push до такта, pull после).

- **Сделай файл** `engine/ecs/include/sky/ecs/object_sync.hpp`
  В файле должны быть: `IEcsObjectSync`, `bind`, `unbind`, `entityOf`, `objectOf`, `pushAuthoringState`, `pullEcsResults`
- **Сделай файл** `engine/ecs/src/object_sync.cpp`
  В файле должны быть: `EcsObjectSyncImpl`, `bind`, `unbind`, `entityOf`, `objectOf`, `invalid`, `pushAuthoringState`, `pullEcsResults`

**На выходе должно получиться (список артефактов):**
- Привязка сущность↔объект; двусторонняя синхронизация вокруг такта; тест в `integrity_tests`.

**Критерий правильности:** Объект, обработанный ECS-системой, получает изменённый трансформ в объектном мире; при паузе не меняется.


---

## Этап 3 (Недели 3–4 (перспектива)). Прикладные системы поверх ECS

**Общее описание задач контура.**

Реализовать первые системы (частицы, пакетная обработка трансформов). ВНИМАНИЕ: файлы ниже в текущем репозитории ОТСУТСТВУЮТ — создаются в этом этапе.


**На выходе должно получиться (список артефактов):**
- Новые файлы `engine/ecs/src/systems/particle_system.cpp` и т.п. (создаются здесь).
- Хотя бы одна визуально наблюдаемая система (частицы) в кадре.

**Критерий правильности:** Пакетная система применяется ко всем сущностям с компонентом за такт; частицы видны.


---

## Этап 4 (Недели 5–6 (перспектива)). Многопоточное исполнение систем

**Общее описание задач контура.**

Использовать планировщик задач ядра для параллельного tick систем.

- **Сделай файл** `engine/core/include/sky/core/job_scheduler.hpp`
  В файле должны быть: `IJobScheduler`, `schedule`, `scheduleAfter`, `wait`
- **Дополни файл** `engine/ecs/src/ecs_world.cpp`
  В файле должны быть: `tick`

**На выходе должно получиться (список артефактов):**
- Параллельный tick независимых систем через `IJobScheduler`.

**Критерий правильности:** Результат детерминирован и совпадает с однопоточным (тест эквивалентности).


---

## Этап 5 (Недели 7–8 (перспектива)). Профилирование систем

**Общее описание задач контура.**

Собрать покадровые тайминги систем и вывести в панель редактора (совместно с E3/E5).

- **Дополни файл** `engine/ecs/src/ecs_world.cpp`
  В файле должны быть: `tick`

**На выходе должно получиться (список артефактов):**
- Тайминги систем измеряются; панель профилировщика в редакторе.

**Критерий правильности:** Длительность такта каждой системы доступна редактору и отображается.
