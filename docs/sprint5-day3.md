# Спринт 5. День 3

## feature/package-tests

- **Исполнитель:** E5 (Пайплайн и QA)
- **Порядок реализации:** 1
- **Зависимости:** `feature/package-resolver`, `feature/package-lock-and-install`

**Цель фичи:** проверить менеджер пакетов — semver, MVS, конфликты, lock round-trip и установку.

**Описание фичи:** тесты резолвера версий, разрешения графа (MVS) и конфликтов, round-trip файла блокировки и установки пакета из трёх источников; закрывает Этап 5 контура E5.

**Общий порядок реализации фичи:**
1. Написать проверки semver, MVS и конфликтов в `package_tests.cpp`.
2. Написать проверки round-trip `sky.lock` и установки из трёх источников.

**Файлы фичи:**
1. `tests/package_tests.cpp`

### Файл: `tests/package_tests.cpp`

**Назначение файла:** тесты менеджера пакетов.

**Пошаговое описание действий:**
1. Проверить semver и `satisfies`.
2. Проверить MVS и обнаружение конфликтов.
3. Проверить round-trip `sky.lock`.
4. Проверить установку из каталога / архива / git.

**Что должно быть в файле:**

*Структуры / классы / enum:* нет.

*Функции / методы:* тестовые функции.

*Логика функций / методов:* semver, MVS, конфликты, lock round-trip, установка из трёх источников.

**Результат по файлу:** зелёный тест `package_tests`.

**Критерий правильности по файлу:**
1. `package_tests` зелёный (semver, MVS, конфликты, lock round-trip, установка).

### На выходе должно получиться

**Список артефактов фичи:**
1. `tests/package_tests.cpp`
2. Тест `package_tests` зелёный.

**Общий критерий правильности:**
1. `package_tests` зелёный (semver, MVS, конфликты, lock round-trip, установка).
2. Пакет ставится из каталога/архива/git; версии резолвятся; `sky.lock` персистит; код пакета работает в Play.
